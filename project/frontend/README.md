# BlindCareNav Frontend

Control web dashboard workspace.

The frontend team can build the admin/control web here and call the backend API at:

```text
http://localhost:8000
```

Recommended local development shape:

```text
frontend/
  package.json
  index.html
  src/
```

Backend-owned files are in `../backend`; avoid changing backend API response shapes without coordinating.
